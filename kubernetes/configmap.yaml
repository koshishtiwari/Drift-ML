apiVersion: v1
kind: ConfigMap
metadata:
  name: drift-ml-config
data:
  security_db_url: "sqlite:///data/security.db"
  kafka_bootstrap_servers: "kafka:9092"
  redis_host: "redis"
  postgres_host: "postgres"
  log_level: "INFO"