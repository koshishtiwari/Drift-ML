# Drift-ML Infrastructure

This directory contains all infrastructure-as-code resources for deploying the Drift-ML platform.

## Directory Structure

- `kubernetes/` - Kubernetes manifests and custom resources
- `terraform/` - Infrastructure provisioning with Terraform
- `helm/` - Helm charts for deploying platform components
- `config/` - Configuration files for various services
